% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{dumbPlot.cell.clustering}
\alias{dumbPlot.cell.clustering}
\title{It draws a Dumbbell plot according condition for each cell cluster identified.}
\usage{
dumbPlot.cell.clustering(fcs.SE, assay.i = "normalized", cell.clusters,
  condition.column, psig.cutoff = 0.05, return.stats = T)
}
\arguments{
\item{fcs.SE}{A FCS.SE object generated through \code{\link[FlowCT:fcs.SE]{FlowCT::fcs.SE()}}.}

\item{assay.i}{Name of matrix stored in the \code{FCS.SE} object from which calculate correlation. Default = \code{"normalized"}.}

\item{cell.clusters}{A vector with clusters identified through \code{\link[FlowCT:fsom.clustering]{FlowCT::fsom.clustering()}} (and, normaly, later renamed).}

\item{condition.column}{Column name from the \code{colData(fcs.SE)} object which contains condition information. De}

\item{psig.cutoff}{P-value cutoff. Default = \code{0.05}.}

\item{return.stats}{Logical indicating if calculated statistics should be returned in a new variable. Default = \code{TRUE}.}
}
\description{
It draws a Dumbbell plot according condition for each cell cluster identified.
}
\examples{
\dontrun{
diffDots.cell.clustering(fcs.SE = fcs_se, cell.clusters = fcs_se$SOM_named, return.stats = F)
}
}
\keyword{Dumbbell}
\keyword{differential}
\keyword{dotplot}
\keyword{longitudinal}
\keyword{plot}
